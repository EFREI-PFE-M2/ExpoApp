import React, { ReactPropTypes } from 'react'
import { View, Text } from '../components/Themed'

export default function Challenge(props: ReactPropTypes) {
  return <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
    <Text>A venir...</Text>
  </View>
}
